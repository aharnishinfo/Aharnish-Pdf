import React, { useState } from 'react';
import { Star, X } from 'lucide-react';
export default function ReviewModal({ onClose }) { const [rating, setRating] = useState(0); const [sent, setSent] = useState(false); return <div className="backdrop review-backdrop"><section className="review-modal"><button className="close" onClick={onClose}><X /></button>{sent ? <><h2>Thank you!</h2><p>Your feedback helps improve Aharnish PDF.</p><button className="process" onClick={onClose}>Close</button></> : <><h2>How was your experience?</h2><p>Your file was downloaded successfully. Please rate Aharnish PDF.</p><div className="stars">{[1,2,3,4,5].map(star => <button key={star} onClick={() => setRating(star)} aria-label={`${star} stars`}><Star fill={star <= rating ? 'currentColor' : 'none'} /></button>)}</div><textarea placeholder="Optional: tell us how we can improve"/><button className="process" disabled={!rating} onClick={() => setSent(true)}>Submit review</button></>}</section></div>; }
